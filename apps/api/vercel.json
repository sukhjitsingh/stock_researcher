{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && bun install",
  "bunVersion": "1.x",
  "buildCommand": "cd ../.. && bun run turbo build --filter=@stock-researcher/api && mkdir -p apps/api/public && echo 'Stock Researcher API' > apps/api/public/index.html",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "crons": [
    { "path": "/api/cron/weekly-scan", "schedule": "0 14 * * 1" }
  ]
}
